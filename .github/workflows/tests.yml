name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.7'
      SERVERPOD_PASSWORD_database: 'pvHt_A4DvbqS7cGAOnPXKJYXCthx8c_X'
      SERVERPOD_PASSWORD_redis: 'jgX9TZ0GKdSsIT7MxqOnQaTyPnAYoMmL'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: smart_todos_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: smart_todos_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: smart_todos_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: smart_todos_server
        run: serverpod generate

      - name: Run tests
        working-directory: smart_todos_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: smart_todos_server
        run: docker compose down -v
